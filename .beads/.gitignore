# SQLite databases (JSONL is source of truth)
*.db
*.db-shm
*.db-wal
*.db-journal

# Daemon runtime files
daemon.log
daemon.pid
daemon.lock
bd.sock
.sync.lock
last-touched
.local_version

# 3-way merge snapshots (local-only)
sync_base.jsonl
*.base.jsonl
*.left.jsonl
*.base.meta.jsonl
*.left.meta.jsonl

# Git merge conflict temp files
*.jsonl[0-9]
*.json[0-9]

# Pollution backup
pollution-backup.jsonl

# Keep JSONL exports (source of truth for git)
!issues.jsonl
!deletions.jsonl
