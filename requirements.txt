# Django Core
Django>=5.1,<6.0

# Configuration
python-decouple>=3.8

# OAuth2
django-oauth-toolkit>=2.0,<3.0

# Background Tasks
django-q2>=1.3,<2.0

# Tree Queries (for asset hierarchy)
django-mptt>=0.16,<1.0

# HTTP Client
requests>=2.31,<3.0

# Cryptography (for token encryption)
cryptography>=41.0,<42.0

# WSGI Server
gunicorn>=21.0,<24.0

# Redis (optional broker for django-q2, but needed to avoid import errors)
redis>=5.0,<6.0
markdown>=3.0
